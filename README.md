### Реализованы и протестированы функции создания классов


## 1. Установка
Склонируйте репозиторий на локальную машину:
https://github.com/MyachinRoma/PythonHomeWork_14.git

2. Установите необходимые зависимости:
pip install -r requirements.txt

## Модуль classess:
В этом модуле созданы функции для классов Product и Category.

## Пример использования предоставлен в модуле main:
Где все работает и все выводится согласно ТЗ.

## В модуле test_classess:
Произведены тесты модуля classess для написанных в нем функций.
Проверяется после установки зависимости библиотеки тестов
poetry add --group dev pytest

## Homework_14.2 Режимы доступа
## Задачи
## Задание 1.
Для класса Category сделаy список товаров приватным атрибутом, чтобы к нему нельзя было получить доступ извне. Для добавления товаров в категорию реализован специальный метод add_product() в классе Category, в который нужно передавать объект класса Product и уже его записывать в приватный атрибут списка товаров.

## Задание 2.
Реализован геттер, который выводит список товаров в виде строк в формате: Название продукта, 80 руб. Остаток: 15 шт.

## Задание 3.
Для класса Product создан класс-метод new_product, который принимает на вход параметры товара в словаре и возвращает созданный объект класса Product.

## Задание 4.
Для класса Product сделан атрибут цены приватным и описаны геттеры и сеттеры. В сеттере реализована проверка: в случае если цена равна или ниже нуля, выводите сообщение в консоль “Цена не должна быть нулевая или отрицательная”, при этом новую цену устанавливать не нужно.

## Задание 5.
Написаны тесты для нового функционала. При этом убедились что тесты, которые были написаны ранее выполняются без ошибок.

## Homework_15.1 Магические методы
## Задачи
## Задание 1
Для класса Product добавлены строковые отображения в следующем виде: Название продукта, 80 руб. Остаток: 15 шт. В предыдущей домашке вы делали геттер для класса Category для вывода списка товаров, теперь каждый продукт имеет реализованное строковое отображение. Вы можете вернуться к этому геттеру и оптимизировать его работу, просто преобразовав объект продукта в строку. Для класса Category добавить строковое отображение в следующем виде:
Название категории, количество продуктов: 200 шт.

## Задание 2
Для удобства работы с продуктами реализована возможность их складывать. Логика сложения работает так, чтобы в итоге у нас получалась полная стоимость всех товаров на складе.
Например, для товара a с ценой 100 рублей и количеством на складе 10 и товара b с ценой 200 рублей и количеством на складе 2 результатом выполнения операции a + b стало, полученное из 100 * 10 + 200 * 2 = 1400.

## Задание 3
Написаны тесты для нового функционала. При этом убедились что тесты, которые были написаны ранее выполняются без ошибок.


## Homework_16.1 Наследование
## Задачи
## Задание 1
Для магазина выделили две категории товаров и создали под них классы:

Смартфон(Smartphone) Помимо имеющихся свойств, добавили следующие:
производительность (efficiency),
модель (model),
объем встроенной памяти (memory),
цвет (color).
Трава газонная (LawnGrass). Помимо имеющихся свойств, добавили следующие:
страна-производитель ( country),
срок прорастания (germination_period),
цвет (color).
Эти оба класса являются классами-наследниками от исходного классаProduct.

## Задание 2.
Доработан функционал сложения таким образом, что можно складывать товары только из одинаковых классов продуктов.

То есть новый функционал не даёт возможность сложить смартфон и траву газонную, вместо этого выводится ошибка TypeError .

## Задание 3
Ранее мы реализовали отдельный метод, с помощью которого можно добавлять объекты продуктов в категории. Теперь защитили метод так, чтобы, кроме смартфонов, травы газонной или других продуктов, в список нельзя добавлять ничего другого.

Доработан метод, который добавляет продукт в категорию, таким образом, что нельзя добавить вместо продукта или его наследников любой другой объект.

## Задание 4
Написаны тесты для нового функционала. При этом убедились что тесты, которые были написаны ранее выполняются без ошибок.

## 16.2 Множественное наследование
## Задачи
## Задание 1
Создаy базовый абстрактный класс с именем BaseProduct, который стаk родительским для класса продуктов. Классы «Смартфон» и «Трава газонная» остаются наследниками класса Product и тем самым наследуют все свойства абстрактного класса. Выделен общий функционал, который есть у каждого продукта, и описан в абстрактном классе.

## Задание 2
Реализован класс-миксин, который при создании объекта, то есть при работе метода __init__, печатает в консоль информацию о том, от какого класса и с какими параметрами создан объект.

Например:

Product('Продукт1', 'Описание продукта', 1200, 10)

Миксин добавлен в цепочку наследования класса Product.

## Задание 3
Написаны тесты для нового функционала. При этом убедились что тесты, которые были написаны ранее выполняются без ошибок.
